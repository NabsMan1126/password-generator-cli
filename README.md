# password-generator-cli
A Python command-line app to generate strong random passwords.
## Description

This is a command-line password generator built with Python that creates strong, random passwords based on user preferences. Users can choose the password length and whether to include numbers and special characters.

The project demonstrates the use of Python’s `random` and `string` modules, along with user input and conditional logic to customize output.
